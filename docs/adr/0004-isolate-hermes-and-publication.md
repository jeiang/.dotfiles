# Isolate Hermes from publication authority

This decision, and the code it governs, moved to the `hermes-agent-config`
repository. See `docs/adr/` there for the current text.

Its Observed Snapshot claims were superseded and corrected in that
repository (see its ADR 0007 and the corrections note on its own copy of
this ADR), so this stale copy should not be relied on.
